#ifndef ACTION_H
#define ACTION_H

/* enum used to indicate possible action that can be done on a directory_entry*/
enum Action { ADD_FILE, ADD_DIRECTORY, UPDATE, DELETE, CANCELLED};

#endif //ACTION_H
